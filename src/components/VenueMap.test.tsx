import { describe, it, expect } from "vitest";
import VenueMap from "./VenueMap";

describe("VenueMap Component", () => {
  it("should export VenueMap for zone interactions", () => {
    expect(VenueMap).toBeDefined();
  });
});
